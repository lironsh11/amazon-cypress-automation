/**
 * TypeScript declaration for cypress-mochawesome-reporter plugin
 * Resolves import errors in cypress.config.ts for HTML report generation
 */
declare module 'cypress-mochawesome-reporter/plugin';